# Copy to .env and fill in your keys, then: source .env

# VirusTotal API key — free tier at https://virustotal.com
# 4 requests/minute, 500/day on free tier
# Without this key, VirusTotal checks are skipped (all other signals still work)
VIRUSTOTAL_API_KEY=your-vt-key-here
